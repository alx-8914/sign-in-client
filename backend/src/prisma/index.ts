import { PrismaClient } from "../generated/prisma";

const prismaClient = new PrismaClient();

export default prismaClient;

// You can add your Prisma queries here and export them for use in other parts of your application.
